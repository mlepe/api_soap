{% extends 'base.html.twig' %}

{% block title %}Membregroupe{% endblock %}

{% block body %}
    <h1>Membregroupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membregroupe.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('membregroupe_index') }}">back to list</a>

    <a href="{{ path('membregroupe_edit', {'id': membregroupe.id}) }}">edit</a>

    {{ include('membregroupe/_delete_form.html.twig') }}
{% endblock %}
