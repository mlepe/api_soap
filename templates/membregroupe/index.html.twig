{% extends 'base.html.twig' %}

{% block title %}Membregroupe index{% endblock %}

{% block body %}
    <h1>Membregroupe index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for membregroupe in membregroupes %}
            <tr>
                <td>{{ membregroupe.id }}</td>
                <td>
                    <a href="{{ path('membregroupe_show', {'id': membregroupe.id}) }}">show</a>
                    <a href="{{ path('membregroupe_edit', {'id': membregroupe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('membregroupe_new') }}">Create new</a>
{% endblock %}
